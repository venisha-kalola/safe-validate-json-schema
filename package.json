{
  "name": "safe-validate-json-schema",
  "version": "0.1.0",
  "description": "Validate JSON against a JSON Schema and return human-friendly, prioritized fix suggestions (masks secrets).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": { "safe-validate-json-schema": "dist/cli.js" },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist && rm -rf node_modules/.cache",
    "test": "vitest",
    "lint": "eslint . --ext .ts",
    "prepare": "npm run build"
  },
  "keywords": ["json", "json-schema", "validate", "schema", "cli", "validation", "ajv", "errors"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "eslint": "^8.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "minimist": "^1.2.8"
  }
}